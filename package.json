{
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc --build",
    "start": "npx ts-node .",
    "clean": "tsc --build --clean"
  },
  "devDependencies": {
    "ts-node": "^10.9.1"
  }
}
